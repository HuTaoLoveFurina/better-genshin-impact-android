opencv-python-headless>=4.9.0
numpy>=1.26.0
PyYAML>=6.0.1

# OCR 引擎。rapidocr 3.x 支持 lang= 多语言切换（日语/韩语/法语/俄语/泰语等）；
# 若你的 Python < 3.13，也可改用旧包 rapidocr-onnxruntime>=1.3（无多语言，仅中英文）。
# 注意：多语言识别模型会在首次使用时自动下载。
rapidocr>=3.0.0
onnxruntime>=1.17.0
