opencv-python-headless>=4.9.0
numpy>=1.26.0
PyYAML>=6.0.1

# OCR engine. rapidocr 3.x supports selectable recognition models for multiple languages.
# On Python versions below 3.13, the legacy rapidocr-onnxruntime package remains an option,
# but it cannot select language-specific models. Missing models may download on first use.
rapidocr>=3.0.0
onnxruntime>=1.17.0
